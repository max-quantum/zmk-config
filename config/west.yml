manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: main   # TEMP diagnostic: test default settings backend on latest ZMK (was fff185e...)
      import: app/west.yml
  self:
    path: config
